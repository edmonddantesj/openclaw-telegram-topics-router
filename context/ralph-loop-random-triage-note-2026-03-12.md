# Ralph Loop ↔ random triage note

Status: ACTIVE
Recorded: 2026-03-12
Source topic: `random` (81)
Target topic: `ralph-loop` (68)

## Intent
`random` is a mixed holding area. Ralph Loop should not absorb the whole topic, but should pull out only work that is better handled as structured repeated operations.

## What stays in random
- one-off mixed discussions
- temporary holding of unclassified ideas
- conversational overflow before classification

## What should be pulled into Ralph Loop
1. repeated operational chores
2. work that needs decomposition + proof routing
3. cross-topic tasks that are stuck because they are still too mixed
4. items that should be converted into dedicated HF/task artifacts

## Triage rule
When random contains a recurring or multi-step execution problem:
- classify it,
- move it to a proper topic or Ralph Loop,
- create HF/task artifact,
- leave only a lightweight pointer behind.

## Proof / references
- `context/topic-state/random.md`
- `context/topics/random_PLAYBOOK_V0_1.md`
