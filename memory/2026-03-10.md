- 2026-03-10 11:54 (현재 상태 저장):
  - Telegram direct/main 응답은 정상 동작 중.
  - 오늘 11:42~11:43경 Bazaar 토픽(topic:55, analyzer/청뇌)에서 Telegram `sendMessage failed` / `polling stall detected` 로그가 발생해 응답이 유실된 것으로 보임.
  - analyzer(청뇌) 자체는 죽은 상태가 아니라 active 흔적이 있으며, 문제 축은 agent 정지보다 Telegram 전송 불안정 쪽으로 판단.
  - direct 채널 health는 현재 `Telegram OK`, gateway reachable 상태.
  - 사용자가 “현재를 저장” 요청하여 본 상태를 일일 메모리에 기록함.

- 2026-03-10 19:33~19:38 (외장 드라이브 미디어 이전 완료):
  - 기존 `~/.openclaw/media` 내용을 `/Volumes/Bitcoin Core Qt/OpenClawMedia/state-media`로 이전하고 symlink 연결 완료.
  - `~/workspace/media`는 `/Volumes/Bitcoin Core Qt/OpenClawMedia/workspace-media`로 symlink 연결 완료.
  - 기존 미디어 약 `39M` 이전 확인.
  - 테스트 이미지 수신 후 inbound 경로가 symlink된 media 경로를 통해 정상 처리되는 상태 확인.
  - 추가로 다운로드 전용 폴더 생성:
    - `/Volumes/Bitcoin Core Qt/OpenClawMedia/downloads`
    - `/Volumes/Bitcoin Core Qt/OpenClawMedia/workspace-media/downloads`
  - 앞으로 외부 다운로드(예: Google Drive 링크, direct file URL)는 가능하면 위 외장 드라이브 경로로 명시 저장하는 운영 방침으로 정리.
